import styled from "@emotion/styled";

export const HeaderPhonebook = styled.header`
  margin-bottom: 20px;
  padding: 15px;
  background-color: var(--bg-color);
  border-radius: 6px;
  box-shadow: 1px 1px 10px -2px var(--main-color);

  display: flex;
  justify-content: space-between;
`;
